WSGI Port
=========

To port to WSGI, we have to do the following:

1) Convert from running on mod_python to some WSGI-FCGI framework
2) Replace the mod_python ``req`` with some paste magic
3) Remove uses of ``req.content_type``, ``req.form``, ``req.write``, and
   ``req.status``
4) Test

